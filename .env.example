# Optional Google account sign-in for forms that require authentication.
GOOGLE_FORM_EMAIL=
GOOGLE_FORM_PASSWORD=

# Where parsed JSON files and browser artifacts will be written.
GOOGLE_FORM_OUTPUT_DIR=./FORMS

# true = keep the browser window hidden during live parsing.
GOOGLE_FORM_HEADLESS=true
